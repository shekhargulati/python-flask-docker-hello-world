version: '3.7'

services:
  hello-world:
    build:
      context: .
      dockerfile: Dockerfile
      container_name: hello-world
    ports:
      - 5001:5000
    environment:
      #- FLASK_APP=app/__init__.py
      - FLASK_ENV=development
